{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npm install && ionic build --prod",
          "outputDirectory": "www"
        }
      }
    ]
  }
  